/*Modifier la fonction passer ArtisteSuivant() pour qu’elle ne fasse rien après le dernier passage.*/
function ArtisteSuivant() public
{
  if(tour <= 10) 
  {
          return tour+=1;
  } 
  else (tour>= 10) 
  {
          return "Merci c'est fini. A bientot"; 
  }
}
